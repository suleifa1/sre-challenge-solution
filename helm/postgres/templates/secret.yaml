apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  username: {{ required "postgres.auth.username is required"  .Values.postgres.auth.username | b64enc }}
  password: {{ required "postgres.auth.password is required"  .Values.postgres.auth.password | b64enc }}